action:
  command: add
  arguments:
    - - awesome-config



/target/.commitlintrc.yml: |
  rules:
    type-enum:
      - 2
      - always
      -
        - build
        - ci
        - docs



/registry/artifact-awesome-config/package.json: |
  {
    "name": "artifact-awesome-config",
    "version": "1.0.0"
  }
/registry/artifact-awesome-config/configs/.commitlintrc.yml: |
  rules:
    body-max-line-length:
      - 2
      - always
      - 200
    type-enum:
      - 2
      - always
      -
        - build
        - ci
        - docs



/merged/.artifactrc.yml: |
  $schema: https://raw.githubusercontent.com/zokugun/artifact/v0.11.0/schemas/v3/install.json
  artifacts:
    artifact-awesome-config:
      version: 1.0.0
/merged/.commitlintrc.yml: |
  rules:
    body-max-line-length:
      - 2
      - always
      - 200
    type-enum:
      - 2
      - always
      - - build
        - ci
        - docs
